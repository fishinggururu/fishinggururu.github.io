---
layout: null
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <generator uri="http://jekyllrb.com" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ page.url | prepend: site.url }}" rel="self" type="application/atom+xml" />
  <link href="{{ site.url }}/" rel="alternate" type="text/html" />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}/</id>
  <title>{{ site.title | strip_html | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <author>
    <name>{{ site.author | xml_escape }}</name>
    <email>{{ site.email | xml_escape }}</email>
  </author>
  {% for post in site.posts limit: 10 %}
    <entry>
      <title>{{ post.title | markdownify | strip_html | strip_newlines | xml_escape }}</title>
      <link href="{{ post.url | prepend: site.url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
      <published>{{ post.date | date_to_xmlschema }}</published>
      {% if post.last_modified_at %}
        <updated>{{ post.last_modified_at | date_to_xmlschema }}</updated>
      {% else %}
        <updated>{{ post.date | date_to_xmlschema }}</updated>
      {% endif %}
      <id>{{ post.url | prepend: site.url | xml_escape }}</id>
      <content type="html" xml:base="{{ post.url | prepend: site.url | xml_escape }}">{{ post.content | markdownify | xml_escape }}</content>
      {% if post.category %}
        <category term="{{ post.category | xml_escape }}" />
      {% endif %}
    </entry>
  {% endfor %}
</feed>
